version: 1.0
runtime: nodejs18

build:
  commands:
    build:
      - yarn install
      - yarn build

run:
  command: yarn start
  network:
    port: 1337

healthcheck:
  path: /_health
  interval: 20
  timeout: 10
